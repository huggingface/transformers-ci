name: model jobs (OTEL-instrumented)

# =============================================================================
# Reusable model-test job for the daily (scheduled) CI, ported from
# huggingface/transformers .github/workflows/model_jobs.yml.
#
# Difference vs the upstream model_jobs.yml: the pytest invocation is wrapped
# with `configure-ci-otel -- python3 -m pytest ...` so each test emits an OTEL
# span to the pytest-observability dashboards (same mechanism as PR CI). All
# `--make-reports` behaviour is preserved unchanged, so Slack reporting, new-
# failure detection and the daily-CI HF dataset upload keep working.
#
# The wrapper is a no-op when the runner does not inject OTEL_EXPORTER_OTLP_
# ENDPOINT: in that case the pytest command is byte-for-byte identical to the
# upstream one (see the OTEL_WRAP guard in "Run all tests on GPU").
# =============================================================================

on:
  workflow_call:
    inputs:
      folder_slices:
        required: true
        type: string
      machine_type:
        required: true
        type: string
      slice_id:
        required: true
        type: number
      docker:
        required: true
        type: string
      commit_sha:
        required: false
        type: string
      report_name_prefix:
        required: false
        default: run_models_gpu
        type: string
      runner_type:
        required: false
        type: string
      report_repo_id:
        required: false
        type: string
      pytest_marker:
        required: false
        type: string
      ci_event:
        required: false
        default: Daily CI
        type: string

env:
  HF_HOME: /mnt/cache
  TRANSFORMERS_IS_CI: yes
  OMP_NUM_THREADS: 8
  MKL_NUM_THREADS: 8
  RUN_SLOW: yes
  # For gated repositories, we still need to agree to share information on the Hub repo. page in order to get access.
  # This token is created under the bot `hf-transformers-bot`.
  HF_TOKEN: ${{ secrets.HF_HUB_READ_TOKEN }}
  TF_FORCE_GPU_ALLOW_GROWTH: true
  CUDA_VISIBLE_DEVICES: 0,1
  NO_COLOR: 1
  # Cap the CPU RAM that psutil reports to pytest so device_map="auto" offloads some layers to disk
  # instead of packing everything into CPU RAM (K8S runners share a ~750 GB host, causing GPU OOM).
  # See huggingface/transformers#46539 for details.
  CI_CPU_MEMORY_LIMIT_GB: 60

permissions:
  contents: read

jobs:
  run_models_gpu:
    name: " "
    strategy:
      max-parallel: 8
      fail-fast: false
      matrix:
        folders: ${{ fromJson(inputs.folder_slices)[inputs.slice_id] }}
    runs-on:
      group: '${{ inputs.machine_type }}'
    container:
      image: ${{ inputs.docker }}
      options: --gpus all --shm-size "16gb" --ipc host -v /mnt/cache/.cache/huggingface:/mnt/cache/
    env:
      # OTEL observability (same knobs as the PR-CI reusable test job).
      # TRANSFORMERS_TEST_OTEL_SUITE groups all spans of this run under the job
      # family (e.g. run_models_gpu) on the dashboards. There is no PR for the
      # daily CI, so `pr` defaults to "none" and the run is identified by
      # GITHUB_RUN_ID on branch `main` (the "branch" dashboard variant).
      TRANSFORMERSCI_OTEL_DEBUG: "1"
      TRANSFORMERS_TEST_OTEL_SUITE: ${{ inputs.report_name_prefix }}
      # Distinguish scheduled daily runs from push-to-main merge runs on the
      # dashboards: both otherwise collapse to pr="main" (no PR number).
      # The "Run all tests on GPU" step slugifies this into
      # transformers.test.ci_event; configure-ci-otel folds it into the span
      # resource attributes and the trace-exporter promotes it to the `ci_event`
      # Prometheus label consumed by the daily dashboard. Every caller family
      # (daily, nightly, past, flash-attn, PR comment CI, push) therefore lands
      # on its own label value instead of all reporting as "daily".
      CI_EVENT: ${{ inputs.ci_event }}
    outputs:
      machine_type: ${{ steps.set_machine_type.outputs.machine_type }}
    steps:
      - name: Echo input and matrix info
        shell: bash
        env:
          folder_slices: ${{ inputs.folder_slices }}
          matrix_folders: ${{ matrix.folders }}
          slice_data: ${{ toJson(fromJson(inputs.folder_slices)[inputs.slice_id]) }}
        run: |
          echo "$folder_slices"
          echo "$matrix_folders"
          echo "$slice_data"

      - name: Echo folder ${{ matrix.folders }}
        shell: bash
        # For folders like `models/bert`, set an env. var. (`matrix_folders`) to `models_bert`, which will be used to
        # set the artifact folder names (because the character `/` is not allowed).
        env:
          matrix_folders_raw: ${{ matrix.folders }}
        run: |
          echo "$matrix_folders_raw"
          matrix_folders="${matrix_folders_raw/'models/'/'models_'}"
          echo "$matrix_folders"
          echo "matrix_folders=$matrix_folders" >> $GITHUB_ENV

      - name: Update clone
        working-directory: /transformers
        env:
          commit_sha: ${{ inputs.commit_sha || github.sha }}
        run: |
          git fetch origin "$commit_sha" && git checkout "$commit_sha"

      - name: Reinstall transformers in edit mode (remove the one installed during docker image build)
        working-directory: /transformers
        run: python3 -m pip uninstall -y transformers && python3 -m pip install -e .

      # Install the CI observability wrapper (provides `configure-ci-otel`,
      # `report-ci-start` and the `transformersci_otel` pytest plugin). Kept as a
      # runtime install so an incident in transformers-ci can be hot-fixed
      # without rebuilding the daily-CI docker image (mirrors the PR reusable).
      - name: Install observability wrapper (transformers-ci[otel])
        working-directory: /transformers
        run: python3 -m pip install 'transformers-ci[otel] @ git+https://github.com/huggingface/transformers-ci@main'

      - name: Update / Install some packages (for Past CI)
        if: ${{ contains(inputs.docker, '-past-') }}
        working-directory: /transformers
        run: |
          python3 -m pip install -U datasets

      - name: Update / Install some packages (for Past CI)
        if: ${{ contains(inputs.docker, '-past-') && contains(inputs.docker, '-pytorch-') }}
        working-directory: /transformers
        run: |
          python3 -m pip install --no-cache-dir git+https://github.com/huggingface/accelerate@main#egg=accelerate

      - name: Install torch 2.14 RC
        working-directory: /transformers
        run: |
          python3 -m pip uninstall -y torch torchvision torchaudio torchcodec
          python3 -m pip install --no-cache-dir "torchcodec" --index-url https://download.pytorch.org/whl/test/cpu
          python3 -m pip install --no-cache-dir "torchaudio" --index-url https://download.pytorch.org/whl/cu130
          python3 -m pip install --no-cache-dir "torch" "torchvision" --index-url https://download.pytorch.org/whl/test/cu130

      - name: NVIDIA-SMI
        run: |
          nvidia-smi

      - name: Environment
        working-directory: /transformers
        run: |
          python3 utils/print_env.py

      - name: Show installed libraries and their versions
        working-directory: /transformers
        run: pip freeze

      - name: Set `machine_type` for report and artifact names
        id: set_machine_type
        working-directory: /transformers
        shell: bash
        env:
          input_machine_type: ${{ inputs.machine_type }}
        run: |
          echo "$input_machine_type"

          if [ "$input_machine_type" = "aws-g5-4xlarge-cache" ]; then
            machine_type=single-gpu
          elif [ "$input_machine_type" = "aws-g5-12xlarge-cache" ]; then
            machine_type=multi-gpu
          else
            machine_type="$input_machine_type"
          fi

          echo "$machine_type"
          echo "machine_type=$machine_type" >> $GITHUB_ENV
          echo "machine_type=$machine_type" >> $GITHUB_OUTPUT

      - name: Create report directory if it doesn't exist
        shell: bash
        env:
          report_name_prefix: ${{ inputs.report_name_prefix }}
        run: |
          mkdir -p "/transformers/reports/${machine_type}_${report_name_prefix}_${matrix_folders}_test_reports"
          echo "dummy" > "/transformers/reports/${machine_type}_${report_name_prefix}_${matrix_folders}_test_reports/dummy.txt"
          ls -la "/transformers/reports/${machine_type}_${report_name_prefix}_${matrix_folders}_test_reports"

      - name: Run all tests on GPU
        working-directory: /transformers
        env:
          report_name_prefix: ${{ inputs.report_name_prefix }}
          pytest_marker: ${{ inputs.pytest_marker }}
          model: ${{ matrix.folders }}
        run: |
          # Map short names to actual test paths for trainer/distributed tests
          test_path="tests/${model}"
          if [ "$model" = "fsdp" ]; then
            test_path="tests/trainer/distributed/test_trainer_distributed_fsdp.py"
          elif [ "$model" = "ddp" ]; then
            test_path="tests/trainer/distributed/test_trainer_distributed_ddp.py"
          elif [ "$model" = "trainer" ] && [ "$report_name_prefix" = "run_trainer_and_fsdp_gpu" ]; then
            test_path="tests/trainer --ignore=tests/trainer/distributed"
          fi

          # ── CI observability (OTEL) ─────────────────────────────────────────
          # OTEL_EXPORTER_OTLP_ENDPOINT / OTEL_TOKEN are injected by the self-
          # hosted runner. When absent, OTEL_WRAP stays empty and the pytest
          # command below is identical to the un-instrumented upstream one.
          # Stamp the hardware name (single-gpu / multi-gpu) so the exporter can
          # split this test_job per hardware and the dashboards map it to GPU/xGPU.
          # $machine_type is set by the "Set machine_type" step above.
          #
          # $CI_EVENT is the caller's human-readable ci_event ("Daily CI",
          # "Nightly CI", "PR Comment CI", "Past CI - pytorch-1.13", "push").
          # Slugify it into a stable, low-cardinality label value; the trailing
          # " ci" is dropped so "Daily CI" keeps emitting exactly "daily" and
          # existing dashboard queries are unchanged.
          ci_event_slug="$(printf '%s' "$CI_EVENT" \
            | tr '[:upper:]' '[:lower:]' \
            | sed -E -e 's/ ci$//' -e 's/[^a-z0-9._]+/-/g' -e 's/^-+//' -e 's/-+$//')"
          [ -n "$ci_event_slug" ] || ci_event_slug="daily"
          echo "ci_event=${CI_EVENT} -> ci_event_slug=${ci_event_slug}"
          export OTEL_RESOURCE_ATTRIBUTES="transformers.test.ci_event=${ci_event_slug},transformers.test.hardware=${machine_type}"

          OTEL_WRAP=""
          if [ -n "$OTEL_EXPORTER_OTLP_ENDPOINT" ]; then
            # Confirm the collector is reachable (diagnostic only, never fatal).
            configure-ci-otel \
              --protocol http \
              --service-name pytest-observability \
              --otlp-endpoint "$OTEL_EXPORTER_OTLP_ENDPOINT" \
              --staging-endpoint 10.90.52.50:5317 \
              --staging-protocol grpc \
              --ping-server || echo "OTEL PING WARNING configure-ci-otel --ping-server is unavailable in this environment"

            # Emit a run-start span so the run/job is visible on the live
            # dashboards before the first test span arrives.
            configure-ci-otel \
              --suite "$TRANSFORMERS_TEST_OTEL_SUITE" \
              --service-name pytest-observability \
              --protocol http \
              --otlp-endpoint "$OTEL_EXPORTER_OTLP_ENDPOINT" \
              --token "$OTEL_TOKEN" \
              --staging-endpoint 10.90.52.50:5317 \
              --staging-protocol grpc \
              -- report-ci-start || echo "OTEL START WARNING report-ci-start is unavailable in this environment"

            # Prefix inserted before `python3 -m pytest`. Values are single
            # tokens (no spaces) so unquoted expansion inside the `script -c`
            # string below is safe.
            OTEL_WRAP="configure-ci-otel --suite $TRANSFORMERS_TEST_OTEL_SUITE --service-name pytest-observability --protocol http --otlp-endpoint $OTEL_EXPORTER_OTLP_ENDPOINT --token $OTEL_TOKEN --staging-endpoint 10.90.52.50:5317 --staging-protocol grpc --"
          fi

          script -q -c "PATCH_TESTING_METHODS_TO_COLLECT_OUTPUTS=yes _PATCHED_TESTING_METHODS_OUTPUT_DIR=/transformers/reports/${machine_type}_${report_name_prefix}_${matrix_folders}_test_reports $OTEL_WRAP python3 -m pytest -rsfE -v -m '${pytest_marker}' --make-reports=${machine_type}_${report_name_prefix}_${matrix_folders}_test_reports ${test_path}" test_outputs.txt
          ls -la
          # Extract the exit code from the output file
          EXIT_CODE=$(tail -1 test_outputs.txt | grep -o 'COMMAND_EXIT_CODE="[0-9]*"' | cut -d'"' -f2)
          exit ${EXIT_CODE:-1}

      - name: Failure short reports
        if: ${{ failure() }}
        # This step is only to show information on Github Actions log.
        # Always mark this step as successful, even if the report directory or the file `failures_short.txt` in it doesn't exist
        continue-on-error: true
        env:
          report_name_prefix: ${{ inputs.report_name_prefix }}
        run: cat "/transformers/reports/${machine_type}_${report_name_prefix}_${matrix_folders}_test_reports/failures_short.txt"

      - name: Captured information
        if: ${{ failure() }}
        continue-on-error: true
        env:
          report_name_prefix: ${{ inputs.report_name_prefix }}
        run: |
          cat "/transformers/reports/${machine_type}_${report_name_prefix}_${matrix_folders}_test_reports/captured_info.txt"

      - name: Copy test_outputs.txt
        if: ${{ always() }}
        continue-on-error: true
        env:
          report_name_prefix: ${{ inputs.report_name_prefix }}
        run: |
          cp /transformers/test_outputs.txt "/transformers/reports/${machine_type}_${report_name_prefix}_${matrix_folders}_test_reports"

      - name: "Test suite reports artifacts: ${{ env.machine_type }}_${{ inputs.report_name_prefix }}_${{ env.matrix_folders }}_test_reports"
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: ${{ env.machine_type }}_${{ inputs.report_name_prefix }}_${{ env.matrix_folders }}_test_reports
          path: /transformers/reports/${{ env.machine_type }}_${{ inputs.report_name_prefix }}_${{ env.matrix_folders }}_test_reports

  collated_reports:
    name: Collated Reports
    if: ${{ always() && inputs.runner_type != '' }}
    needs: run_models_gpu
    uses: huggingface/transformers/.github/workflows/collated-reports.yml@main
    with:
      job: run_models_gpu
      report_repo_id: ${{ inputs.report_repo_id }}
      gpu_name: ${{ inputs.runner_type }}
      machine_type: ${{ needs.run_models_gpu.outputs.machine_type }}
    secrets: inherit
